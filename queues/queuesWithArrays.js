// First think in must be the first think out

// -------------------------------------------
// using arrays

// Must use push and shift together

let queue = []

queue.push('FIRST')
queue.push('SECOND')
queue.push('THIRD')

queue.shift()
queue.shift()
queue.shift()

// Must use unshift and pop together

let queue2 = []

queue2.unshift('FIRST')
queue2.unshift('SECOND')
queue2.unshift('THIRD')

queue2.pop()
queue2.pop()
queue2.pop()